# 暖光工作台视觉重设计 + 夜间模式 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `index.html` 的视觉外壳与 canvas 配色重做为「暖光工作台」风格，并新增可记忆、默认跟随系统的白天/夜间双主题，盘面默认居中铺满舞台。

**Architecture:** 纯视觉层改动，全部集中在单文件 `index.html`。颜色收敛到 `:root` CSS 变量 + `[data-theme="night"]` 覆盖块；canvas 绘制读取一份缓存的主题色对象 `TH`（从 CSS 变量取值，主题切换时刷新）。修复 `fit()` 在首屏布局未稳时算错尺寸的时序问题。不动 `server.js` / `sessions.js` / hooks / localStorage 存档结构。

**Tech Stack:** 原生 HTML / CSS / Canvas 2D / 原生 JS，单文件零依赖。`node --test`（既有 `test/sessions.test.js`）作为功能回归守护。`mcp__Claude_Preview__*` 预览截图作为视觉验收。

**验收方式说明（重要）：** 本项目是纯视觉重做，没有针对 UI 的单元测试。每个任务的"验证"步骤采用：①`mcp__Claude_Preview__preview_screenshot` 截图肉眼核对；②`mcp__Claude_Preview__preview_console_logs level=error` 确认无报错；③涉及交互/存档处用 `node --test` 确认既有逻辑测试仍通过。预览服务器在 `.claude/launch.json` 中名为 `pinbeads`（端口 4500）。

---

## 关键事实速查（执行前必读）

`index.html` 当前状态（行号为改动前基准，编辑后会偏移，**按内容定位**）：

- `:root` 变量在第 8–12 行：`--bg:#fbf6ee; --panel:#fffdf9; --ink:#4a4036; --muted:#9a8f80; --line:#ece2d3; --accent:#ff8a5b; --accent2:#ffd23f; --ok:#4cae7e; --wait:#f4a82c; --shadow:0 6px 22px rgba(120,90,50,.10);`
- `body` 背景在第 15–19 行：`background:radial-gradient(1200px 600px at 80% -10%, #fff3df, transparent), var(--bg);`
- 顶栏 `<header>` HTML 在第 258–270 行；当前按钮：`btnPick` `btnGallery` `btnHint` `btnMode`(绘制) `btnErase` `btnCheck` `btnSound`。
- canvas 绘制：`draw()` 第 459 行，空格渲染在 508–514 行；`drawBead()` 第 528 行；`drawPegboard()` 第 589 行（硬编码 `#e8dcc8` / `rgba(140,120,90,.16)`）。
- `mix(hex,t)` 第 456 行（朝白色混合）。
- `fit()` 第 608 行；`loadPattern()` 第 929 行末尾调用 `fit()`（第 938 行）；`boot()` 第 1323 行、末尾 `boot()` 调用第 1336 行。
- CSS 中需要 tokenize 的硬编码颜色（散落全文）：`#fff`、`#fffdf8`、`#faf4ea`、`#fff7ee`、`#fffaf2`、`#f3ecde`、`#f0e8db`、`#fff3df`，以及展示墙木色 `#e8b878/#d99a52/#b9824a` 等。

---

## Task 1：主题地基（CSS 变量 + 夜间块 + 暖光背景 + 切换逻辑）

**Files:**
- Modify: `index.html`（`<style>` 内 `:root`、`body`；`<header>` 加按钮；`<script>` 内加主题初始化逻辑）

- [ ] **Step 1：扩展 `:root` 为完整 token 集（白天值）**

把第 8–12 行的 `:root{...}` 整块替换为下面内容（保留原有变量名与值，新增 desk/board/canvas/glow 等 token）：

```css
:root{
  /* —— 白天（暖光台面）—— */
  --desk:#e6d8c0;            /* 台面基色（比盘面深，制造远近层次）*/
  --desk-glow:#fff4e0;       /* 右上角台灯光晕 */
  --board-base:#f3ece0;      /* 盘面底板（浅硅胶）*/
  --panel:#fffdf8;           /* 卡片/面板 */
  --panel-2:#fff;            /* 更亮的卡片内层（按钮/卡片白底）*/
  --soft:#fff7ee;            /* 选中/hover 暖底 */
  --soft-2:#fffaf2;          /* 次级 hover 暖底 */
  --chip:#f3ecde;            /* 小标签/进度槽底 */
  --bg:#fbf6ee;              /* 兼容旧引用 */
  --ink:#4a4036; --muted:#9a8f80;
  --line:#ece2d3;
  --accent:#ff8a5b; --accent2:#ffd23f;
  --ok:#4cae7e; --wait:#f4a82c; --done:#7d8fb3;
  --shadow:0 6px 22px rgba(120,90,50,.10);
  --shadow-lg:0 14px 40px rgba(120,80,40,.16);
  /* canvas 用（drawPegboard / 空格 / 豆子 AO / 夜间辉光）*/
  --peg-base:#e8dcc8;        /* 洞洞板底 */
  --peg-dot:rgba(140,120,90,.16);
  --cell-target:243,236,224; /* 空格朝此色混合(=board-base 的 rgb) */
  --cell-stroke:rgba(150,130,100,.18);
  --cell-text:rgba(120,100,75,.55);
  --bead-glow:rgba(0,0,0,0); /* 白天无辉光 */
}
/* —— 夜间（暖夜灯，非冷科技黑）—— */
[data-theme="night"]{
  --desk:#221b15; --desk-glow:#4a3318;
  --board-base:#352c23;
  --panel:#2e2620; --panel-2:#372e26;
  --soft:#3a2f24; --soft-2:#342a20;
  --chip:#3d3328;
  --bg:#221b15;
  --ink:#f0e6d6; --muted:#a89880;
  --line:#473c30;
  --accent:#ff9d6b; --accent2:#ffbf7a;
  --ok:#5fc08e; --wait:#f0b154; --done:#8d9ec2;
  --shadow:0 6px 22px rgba(0,0,0,.40);
  --shadow-lg:0 14px 40px rgba(0,0,0,.50);
  --peg-base:#352c23;
  --peg-dot:rgba(255,225,180,.07);
  --cell-target:53,44,35;
  --cell-stroke:rgba(255,225,180,.12);
  --cell-text:rgba(255,225,180,.40);
  --bead-glow:rgba(255,170,110,.55);
}
```

- [ ] **Step 2：把 `body` 背景换成暖光台面 + 加全局过渡**

把第 15–19 行的 `body{...}` 替换为：

```css
body{
  margin:0; font-family:-apple-system,"PingFang SC","Microsoft YaHei",system-ui,sans-serif;
  background:
    radial-gradient(1100px 720px at 82% -12%, var(--desk-glow), transparent 60%),
    var(--desk);
  color:var(--ink); overflow:hidden;
  transition:background-color .28s ease, color .28s ease;
}
/* 主题切换时大面积换色平滑过渡（克制：仅颜色相关属性）*/
header, aside, main, .toolbar, .palette, .scard, .pgroup, .sheet, .swatch, .icon-btn{
  transition:background-color .28s ease, color .28s ease, border-color .28s ease, box-shadow .28s ease;
}
```

- [ ] **Step 3：顶栏加主题切换按钮**

在 `<header>`（第 258–270 行）中，把 `<button class="icon-btn" id="btnSound">🔊</button>` 这一行替换为下面两行（主题按钮在前、声音在后）：

```html
    <button class="icon-btn" id="btnTheme" title="切换白天/夜间">🌙</button>
    <button class="icon-btn" id="btnSound">🔊</button>
```

- [ ] **Step 4：加主题初始化与切换 JS**

在 `<script>` 顶部、`state` 定义（第 432 行 `const state=...`）**之前**插入下面这段（自执行初始化 + 暴露 `applyThemeVars` 占位，真正的 canvas 取值在 Task 3 接入）：

```js
/* ============================== 主题（白天/夜间）============================== */
const THEME_KEY='pinbeads:theme';
function systemPrefersNight(){ try{ return matchMedia('(prefers-color-scheme: dark)').matches; }catch(_){ return false; } }
function currentTheme(){ return document.documentElement.getAttribute('data-theme')==='night'?'night':'day'; }
function applyTheme(t, persist){
  document.documentElement.setAttribute('data-theme', t==='night'?'night':'day');
  const btn=document.getElementById('btnTheme'); if(btn) btn.textContent = t==='night'?'☀️':'🌙';
  if(persist){ try{ localStorage.setItem(THEME_KEY, t); }catch(_){} }
  if(typeof applyThemeVars==='function') applyThemeVars();   // 刷新 canvas 取色（Task 3 定义）
  if(typeof draw==='function') draw();                       // 重绘盘面以应用新色
}
(function initTheme(){
  let saved=null; try{ saved=localStorage.getItem(THEME_KEY); }catch(_){}
  const t = saved || (systemPrefersNight()?'night':'day');
  document.documentElement.setAttribute('data-theme', t);    // DOM 已就绪前先定 html 属性
  // 未手动设置过时，跟随系统变化
  try{ matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e=>{
    let s=null; try{ s=localStorage.getItem(THEME_KEY); }catch(_){}
    if(!s) applyTheme(e.matches?'night':'day', false);
  }); }catch(_){}
})();
```

并在 DOM 就绪后绑定按钮 —— 在 `boot()` 函数体（第 1323 行起）**最前面**加：

```js
  (function(){
    applyTheme(currentTheme(), false);   // 同步按钮图标 + 首次 applyThemeVars
    const tb=document.getElementById('btnTheme');
    if(tb) tb.onclick=()=> applyTheme(currentTheme()==='night'?'day':'night', true);
  })();
```

- [ ] **Step 5：验证**

确保 `pinbeads` 预览服务运行（`mcp__Claude_Preview__preview_start name="pinbeads"`），视口 1280×820。

1. `preview_screenshot` → 白天：背景应是暖木棕 + 右上暖光晕（比原来更有层次）。
2. `preview_eval` 执行 `document.getElementById('btnTheme').click()` → `preview_screenshot` → 整页切到暖深色，按钮变 ☀️，过渡平滑。
3. `preview_eval` 执行 `location.reload()` 后 `preview_screenshot` → 仍是夜间（已记忆）。
4. `preview_console_logs level="error"` → 无报错。
5. 切回白天：`preview_eval` `document.getElementById('btnTheme').click()`。

- [ ] **Step 6：提交**

```bash
git add index.html docs/2026-06-08-视觉重设计-暖光工作台-plan.md
git commit -m "feat(theme): 暖光台面背景 + 白天/夜间双主题地基与切换"
```

---

## Task 2：tokenize 全部硬编码外壳颜色（夜间正确换肤）

**Files:**
- Modify: `index.html`（`<style>` 内多处）

目标：把散落在 CSS 各处的硬编码亮色替换为 Task 1 的变量，使夜间模式下所有面板/卡片/弹窗/展示墙都正确变暗、文字可读。**逐项替换，不改其它属性。**

- [ ] **Step 1：按下表做精确替换**（每条都是"出现处 → 改成"；同一值多处出现的用 Edit `replace_all`，但**先确认上下文语义一致**，展示墙木色除外）：

| 原值（语义） | 改成 |
|---|---|
| `.icon-btn{... background:#fff ...}` | `background:var(--panel-2)` |
| `.swatch{... background:var(--panel) ...}`（保留） | 不变 |
| `.swatch.active{... background:#fff7ee}` | `background:var(--soft)` |
| `.scard{... background:#fff}` | `background:var(--panel-2)` |
| `.pgroup{... background:#fff}` | `background:var(--panel-2)` |
| `.pghead:hover{background:#fffaf2}` | `background:var(--soft-2)` |
| `.pgcount{... background:#f3ecde ...}` | `background:var(--chip)` |
| `.empty-hint code{background:#f3ecde ...}` | `background:var(--chip)` |
| `.filter-btn{... background:#fff ...}` | `background:var(--panel-2)` |
| `.filterpop{... background:#fff ...}` | `background:var(--panel-2)` |
| `.fp-item:hover{background:#fffaf2}` | `background:var(--soft-2)` |
| `.fp-clear{... background:#fff ...}` | `background:var(--panel-2)` |
| `.sheet{background:var(--panel) ...}`（保留） | 不变 |
| `.pcard{... background:#fff}` | `background:var(--panel-2)` |
| `.pcard canvas{... background:#faf4ea}` | `background:var(--board-base)` |
| `.pcard .del{... background:#fff ...}` | `background:var(--panel-2)` |
| `.uploader.drag{... background:#fff7ee}` | `background:var(--soft)` |
| `.seg button{... background:#fff ...}` | `background:var(--panel-2)` |
| `.hangwork .frame{background:#fffdf8; border:5px solid #fff; ...}` | `background:var(--panel); border:5px solid var(--panel-2);` |
| `.hangwork .frame img{... background:#faf4ea}` | `background:var(--board-base)` |
| `.hangwork .del{... background:#fff ...}` | `background:var(--panel-2)` |
| `.keychain .charm{background:#fffdf8; border:4px solid #fff; ...}` | `background:var(--panel); border:4px solid var(--panel-2);` |
| `.keychain .charm img{... background:#faf4ea}` | `background:var(--board-base)` |
| `.keychain .del{... background:#fff ...}` | `background:var(--panel-2)` |
| `.wd-img{... background:#faf4ea ...}` | `background:var(--board-base)` |
| `.wd-info .wd-meta .tag{... background:#f3ecde ...}` | `background:var(--chip)` |
| `.wd-actions button{... background:#fff ...}` | `background:var(--panel-2)` |
| `.progress-wrap{... background:#f0e8db ...}` | `background:var(--chip)` |

- [ ] **Step 2：展示墙 `#showcase` 背景改为台面色**

把 `#showcase{...}`（第 162–167 行）的 `background:` 块替换为：

```css
    background:
      radial-gradient(1100px 500px at 80% -8%, var(--desk-glow), transparent 60%),
      var(--desk);
```

`.sc-head{... background:rgba(255,253,249,.86) ...}` → 改为 `background:color-mix(in srgb, var(--panel) 86%, transparent);`（夜间也能用；若担心兼容可改用两套 token，但目标浏览器为本机 Chromium 预览，`color-mix` 可用）。

> 展示墙的木质洞洞板面板（`.pegboard` `#e8b878/#d99a52/#b9824a`）与挂钩/钥匙环金属色**保持原木色不动**——它是"展示道具"的固定材质，白天夜间都成立，不纳入主题切换。仅其文字色 `.sc-section-label.on-board{color:#6b4a23}` 等保留。

- [ ] **Step 3：验证**

1. 切到夜间，`preview_eval` 打开选图案弹窗：`document.getElementById('btnPick').click()` → `preview_screenshot` → 弹窗、卡片、上传框应为暖深色，文字可读，无刺眼白块。关闭：`preview_eval` `document.querySelector('#pickModal .x').click()`。
2. `preview_eval` 打开作品墙 `document.getElementById('btnGallery').click()` → `preview_screenshot` → 台面暖深、木质洞洞板道具仍为木色（符合预期）。返回：`preview_eval` `document.getElementById('scBack').click()`。
3. 切回白天复核这些面板与原版一致。
4. `preview_console_logs level="error"` 无报错。

- [ ] **Step 4：提交**

```bash
git add index.html
git commit -m "feat(theme): tokenize 外壳/弹窗/面板硬编码色，夜间正确换肤"
```

---

## Task 3：canvas 主题化（洞洞板 / 空格 / 豆子 AO / 夜间辉光）

**Files:**
- Modify: `index.html`（`<script>` 内 `mix`/`draw`/`drawBead`/`drawPegboard` 附近）

- [ ] **Step 1：加主题色缓存与辅助函数**

在 `mix(hex,t)` 定义（第 456 行）**之后**插入：

```js
/* canvas 主题取色：缓存自 CSS 变量，主题切换时由 applyTheme→applyThemeVars 刷新 */
let TH={};
function applyThemeVars(){
  const cs=getComputedStyle(document.documentElement); const g=n=>cs.getPropertyValue(n).trim();
  const tgt=(g('--cell-target')||'243,236,224').split(',').map(n=>+n);
  TH={
    peg:g('--peg-base')||'#e8dcc8',
    pegDot:g('--peg-dot')||'rgba(140,120,90,.16)',
    cellTarget:(tgt.length===3?tgt:[243,236,224]),
    cellStroke:g('--cell-stroke')||'rgba(150,130,100,.18)',
    cellText:g('--cell-text')||'rgba(120,100,75,.55)',
    glow:g('--bead-glow')||'rgba(0,0,0,0)',
  };
}
applyThemeVars();
// 朝指定 rgb 混合（空格预览用；替代只能朝白混合的 mix）
function mixTo(hex, rgb, t){ const [r,g,b]=hx2rgb(hex);
  const m=(v,to)=>Math.round(v+(to-v)*t); return `rgb(${m(r,rgb[0])},${m(g,rgb[1])},${m(b,rgb[2])})`; }
function glowActive(){ return TH.glow && !/,\s*0\s*\)$/.test(TH.glow) && TH.glow!=='rgba(0,0,0,0)'; }
```

- [ ] **Step 2：空格渲染改用主题色**

把 `draw()` 中空格分支（第 508–514 行 `else{ ... }`）替换为：

```js
      }else{
        const isHint = hintColor && hex===hintColor;
        bctx.fillStyle= isHint? mixTo(hex,TH.cellTarget,.45) : mixTo(hex,TH.cellTarget,.80);
        roundRect(px+s*0.06,py+s*0.06,s*0.88,s*0.88,s*0.18); bctx.fill();
        bctx.strokeStyle= isHint? hex : TH.cellStroke;
        bctx.lineWidth= isHint? 2*DPR : 1*DPR; bctx.stroke();
        bctx.fillStyle= isHint? hex : TH.cellText;
        bctx.fillText(String(c+1), px+s/2, py+s/2+ s*0.02);
      }
```

- [ ] **Step 3：洞洞板改用主题色**

把 `drawPegboard()`（第 589–597 行）替换为：

```js
function drawPegboard(x,y,w,h,s){
  bctx.fillStyle=TH.peg;
  roundRect(x-s*0.35,y-s*0.35,w+s*0.7,h+s*0.7,s*0.5); bctx.fill();
  bctx.fillStyle=TH.pegDot;
  const p=state.pattern;
  for(let yy=0;yy<p.height;yy++)for(let xx=0;xx<p.width;xx++){
    bctx.beginPath(); bctx.arc(x+(xx+0.5)*s, y+(yy+0.5)*s, s*0.16, 0, 7); bctx.fill();
  }
}
```

- [ ] **Step 4：夜间豆子加暖色辉光**

在 `drawBead()` 函数体内，**`if(back){...}` 那行之后、`const wall=...` 之前**插入辉光绘制（仅夜间、仅未熔接的立体豆、放在豆体之下）：

```js
  if(glowActive() && f<0.5){
    bctx.save();
    const gr=bctx.createRadialGradient(cx,cy,half*0.2, cx,cy,half*1.7);
    gr.addColorStop(0, TH.glow); gr.addColorStop(1,'rgba(0,0,0,0)');
    bctx.globalCompositeOperation='lighter';
    bctx.fillStyle=gr; bctx.beginPath(); bctx.arc(cx,cy,half*1.7,0,7); bctx.fill();
    bctx.restore();
  }
```

- [ ] **Step 5：验证**

1. 夜间：`preview_screenshot` → 洞洞板为暗暖棕，空格编号在暗底可读，已放置的豆子带柔和暖色辉光。
2. 白天：切回 → 洞洞板/空格与原版基本一致（辉光关闭）。
3. 切换主题后**立即重绘**：`preview_eval` 连点 `btnTheme` 两次，盘面颜色随之更新（验证 `applyThemeVars`+`draw` 串起来了）。
4. `preview_console_logs level="error"` 无报错。

- [ ] **Step 6：提交**

```bash
git add index.html
git commit -m "feat(theme): canvas 洞洞板/空格/豆子主题化 + 夜间暖色辉光"
```

---

## Task 4：修复盘面首屏不居中铺满（消除死白）

**Files:**
- Modify: `index.html`（`boot()` 末尾 / `loadPattern` 附近）

**问题：** `boot()`→`loadPattern()`→`fit()` 在首屏布局未稳定时运行，`stage.clientWidth/Height` 偏小，导致盘面定格在左上小尺寸；之后 `ResizeObserver` 只调 `draw()` 不再 `fit()`。

- [ ] **Step 1：首屏布局稳定后补一次 fit**

在 `boot()` 函数体末尾（`boot()` 内最后一行之后、函数闭合 `}` 之前）加：

```js
  // 首屏：等布局稳定后再适配一次，避免盘面卡在左上角小尺寸
  requestAnimationFrame(()=>requestAnimationFrame(()=>{ if(state.pattern) fit(); }));
```

- [ ] **Step 2：stage 首次出现非零尺寸时自动 fit 一次**

把第 449 行的 ResizeObserver 那行替换为带"首次有效尺寸即 fit"的版本：

```js
if(window.ResizeObserver){
  let _fitted=false;
  new ResizeObserver(()=>{
    resizeCanvas();
    if(!_fitted && stage.clientWidth>50 && stage.clientHeight>50 && state.pattern){ _fitted=true; fit(); }
  }).observe(stage);
}
```

- [ ] **Step 3：验证**

1. `preview_eval` `location.reload()` → 等待 → `preview_screenshot` → 盘面应**居中**且按 `fit` 规则放大（小图案居中带均匀留白，不再缩在左上角）。
2. `preview_resize` 改成 1100×700 再 1400×900，各 `preview_screenshot` → 盘面始终大致居中、无大片单侧死白。
3. 手动缩放后不被强制重置：`preview_eval` 设置 `document.getElementById('zoom').value=10; document.getElementById('zoom').dispatchEvent(new Event('input'))` 再 `preview_screenshot`，确认用户缩放仍生效（`_fitted` 只在首次触发）。
4. `preview_console_logs level="error"` 无报错。

- [ ] **Step 4：提交**

```bash
git add index.html
git commit -m "fix(board): 首屏布局稳定后自动居中适配，消除舞台死白"
```

---

## Task 5：顶栏分主次（导航组 vs 工具组）

**Files:**
- Modify: `index.html`（`<header>` HTML + `<style>` 顶栏相关）

- [ ] **Step 1：重排顶栏 HTML 分组**

把 `<header>`（第 258–270 行）内 spacer 之后的按钮区替换为（用 `.nav-group` 与 `.tool-group` 包裹，工具组做 segmented）：

```html
    <div class="spacer"></div>
    <div class="nav-group">
      <button class="icon-btn nav" id="btnPick">🖼 选图案</button>
      <button class="icon-btn nav" id="btnGallery">🏆 我的作品</button>
    </div>
    <div class="tool-group" role="group" aria-label="工具">
      <button class="tool" id="btnHint" title="高亮当前色">💡</button>
      <button class="tool active" id="btnMode" data-mode="draw" title="绘制">🖌</button>
      <button class="tool" id="btnErase" title="橡皮">🧽</button>
      <button class="tool" id="btnCheck" title="检查">🔍</button>
    </div>
    <button class="icon-btn" id="btnTheme" title="切换白天/夜间">🌙</button>
    <button class="icon-btn" id="btnSound">🔊</button>
```

> 注意：保留所有 `id`，JS 事件绑定不变。`btnMode`/`btnErase`/`btnHint`/`btnCheck` 原先靠 `.icon-btn.active` 类切换选中态；下一步给 `.tool.active` 等价样式，**确认相关 JS 里 toggle 的是 `classList` 而非依赖 `icon-btn`**（见 Step 3 验证）。

- [ ] **Step 2：加顶栏分组样式**

在 `.icon-btn.active{...}`（第 34 行）之后插入：

```css
  .nav-group{display:flex; gap:8px}
  .tool-group{display:inline-flex; border:1px solid var(--line); border-radius:12px;
    background:var(--panel-2); overflow:hidden; box-shadow:var(--shadow)}
  .tool-group .tool{border:0; background:transparent; color:var(--ink); cursor:pointer;
    padding:7px 12px; font-size:15px; line-height:1; transition:.15s; border-left:1px solid var(--line)}
  .tool-group .tool:first-child{border-left:0}
  .tool-group .tool:hover{background:var(--soft)}
  .tool-group .tool.active{background:var(--accent); color:#fff}
```

- [ ] **Step 3：核对工具按钮选中态 JS**

`grep -n "btnMode\|btnErase\|btnHint\|btnCheck\|classList" index.html` 查看这些按钮的点击逻辑。若代码用 `classList.toggle('active')` / `classList.add('active')` 切换，则新样式 `.tool.active` 直接生效，无需改 JS。若发现它依赖 `.icon-btn.active` 选择器做查询（如 `querySelector('.icon-btn.active')`），把对应选择器同步为 `.tool.active`。**按实际代码定位修改，保持行为一致。**

- [ ] **Step 4：验证**

1. `preview_screenshot` → 顶栏：导航（选图案/作品墙）成一组，工具（💡🖌🧽🔍）合成一个分段控件，当前工具高亮。
2. `preview_eval` 依次点 `btnErase`、`btnMode`、`btnCheck`，每次 `preview_screenshot` 确认高亮在分段控件内正确切换、且功能仍工作（橡皮/绘制/检查行为不变）。
3. 白天 + 夜间各截一次，确认分段控件两主题下都清晰。
4. `preview_console_logs level="error"` 无报错。

- [ ] **Step 5：提交**

```bash
git add index.html
git commit -m "feat(header): 导航组与工具分段控件分离，建立操作主次"
```

---

## Task 6：底部操作台 + 调色板「豆子盒」

**Files:**
- Modify: `index.html`（`<style>` 工具栏/调色板；不改 `refreshPalette` 的 DOM 结构）

> `refreshPalette()`（第 685 行）生成的每个 `.swatch` 结构为 `.chip + .num + .left`，**保持结构不变**，只重写样式让它像嵌在凹槽里的实体豆。

- [ ] **Step 1：盘面底板浮起 + 操作台容器质感**

给 `.toolbar` 与 `.palette`（第 52–73 行附近）增强为"操作台"。把 `.toolbar{...}` 与 `.palette{...}` 替换为：

```css
  .toolbar{
    display:flex; align-items:center; gap:10px; padding:9px 16px; background:var(--panel);
    border-top:1px solid var(--line); flex-wrap:wrap; box-shadow:0 -2px 12px rgba(120,80,40,.05);
  }
  .toolbar .title{font-weight:600; margin-right:6px}
  .toolbar input[type=range]{width:130px; accent-color:var(--accent)}
  .palette{display:flex; gap:10px; padding:12px 16px 14px; background:var(--panel);
    border-top:1px solid var(--line); overflow-x:auto}
```

- [ ] **Step 2：调色板做成「豆子盒」凹槽**

把 `.swatch` 相关样式（第 63–73 行）替换为：

```css
  .swatch{
    --c:#ccc; min-width:56px; border:1px solid var(--line); border-radius:14px; padding:7px 4px 6px;
    cursor:pointer; background:var(--chip); text-align:center; transition:.14s; position:relative; user-select:none;
    box-shadow:inset 0 2px 5px rgba(90,60,25,.12);   /* 凹槽内阴影 */
  }
  .swatch:hover{transform:translateY(-1px)}
  .swatch .chip{width:30px; height:30px; border-radius:50%; margin:0 auto 4px; background:var(--c);
    box-shadow:inset -2px -3px 0 rgba(0,0,0,.16), inset 2px 2px 0 rgba(255,255,255,.42), 0 2px 3px rgba(90,60,25,.22)}
  .swatch .num{font-size:11px; color:var(--muted)}
  .swatch .left{font-size:11px; font-weight:600; color:var(--ink)}
  .swatch.active{border-color:var(--accent); background:var(--soft); box-shadow:0 0 0 2px var(--accent) inset}
  .swatch.done{opacity:.45}
  .swatch.done .left::after{content:" ✓"; color:var(--ok)}
```

- [ ] **Step 3：进度条质感**

把 `.progress-bar`（第 59 行）替换为：

```css
  .progress-bar{height:100%; width:0; border-radius:99px;
    background:linear-gradient(90deg,var(--accent2),var(--accent)); transition:width .25s;
    box-shadow:0 0 8px rgba(255,138,91,.45)}
```

- [ ] **Step 4：验证**

1. `preview_screenshot` 白天 → 调色板为凹槽豆子盒、选中色有橙圈、拼完的色变暗带 ✓；进度条暖色发光。
2. 点不同色块 `preview_eval document.querySelectorAll('.swatch')[1].click()` → 截图确认选中态切换正常、拼豆仍按新色放置。
3. 夜间截一次确认豆子盒在暗底成立。
4. `preview_console_logs level="error"` 无报错。

- [ ] **Step 5：提交**

```bash
git add index.html
git commit -m "feat(dock): 调色板做成豆子盒凹槽 + 进度条质感 + 操作台层次"
```

---

## Task 7：收尾打磨 + 全量回归

**Files:**
- Modify: `index.html`（盘面底板浮起、面板浮起阴影微调）

- [ ] **Step 1：盘面底板浮起阴影（强化远近层次）**

把 `.stage`（第 48 行 `.stage{flex:1; position:relative; overflow:hidden}`）替换为，让舞台中央有一块比台面浅、带柔和投影的"工作垫"感（用 CSS 径向叠加，不挡 canvas 交互）：

```css
  .stage{flex:1; position:relative; overflow:hidden;
    background:radial-gradient(60% 70% at 50% 48%, color-mix(in srgb, var(--board-base) 55%, transparent), transparent 75%);}
```

- [ ] **Step 2：右侧面板与卡片浮起**

把 `aside`（第 75 行）的背景与 `.scard`（第 83–86 行）阴影各加一层：`aside{... box-shadow:-2px 0 16px rgba(120,80,40,.05);}`（在 `border-left` 后追加），`.scard{... box-shadow:var(--shadow);}`（追加）。按实际属性串就近 Edit。

- [ ] **Step 3：完成横幅/Toast 适配（确认两主题对比度）**

`#banner`（第 40–44 行）与 `.toast`（第 134 行）已用 `--ok`/`--ink`/`--accent2`，夜间值已在 Task 1 调过。此步仅**验证**：触发一次完成横幅 `preview_eval showBanner('🧩 测试横幅')`（若 `showBanner` 在作用域）→ 截图确认白天/夜间都清晰。

- [ ] **Step 4：功能回归**

```bash
node --test
```
Expected: 既有 `test/sessions.test.js` 全部 PASS（视觉改动不应影响会话状态机）。

- [ ] **Step 5：交互冒烟（预览）**

依次 `preview_eval` 模拟一遍核心流程并截图：选图案弹窗打开/关闭、放几颗豆（`board` 上 `preview_click` 或派发 pointer 事件）、橡皮、检查、切主题、作品墙打开/返回。确认无报错、无错位。`preview_console_logs level="error"` 收尾确认。

- [ ] **Step 6：最终对照截图**

白天 + 夜间各一张 `preview_screenshot` 留档，对照 spec 八条验收标准逐条核对。

- [ ] **Step 7：提交**

```bash
git add index.html
git commit -m "polish(theme): 盘面工作垫层次 + 面板浮起 + 双主题回归收尾"
```

---

## Self-Review（已对照 spec 八条验收）

- 验收①盘面居中铺满 → Task 4。
- 验收②白天远近层次不发灰 → Task 1（台面背景）+ Task 7（工作垫/浮起）。
- 验收③豆子质感（环形高光/投影）→ 现有 `drawBead` 已具备；点亮回弹现有 `popBead`；夜间辉光 → Task 3 Step 4。
- 验收④顶栏导航/工具分组 → Task 5。
- 验收⑤底部操作台 + 豆子盒 → Task 6。
- 验收⑥🌙/☀️ 切换全组件换肤 + 夜间辉光 → Task 1 + Task 2 + Task 3。
- 验收⑦默认跟随系统、手动记忆 → Task 1 Step 4。
- 验收⑧功能/存档不受影响、单文件零依赖 → Task 7 `node --test` + 全程不引入资源/依赖。

类型/命名一致性：`applyTheme`/`applyThemeVars`/`currentTheme`/`TH`/`mixTo`/`glowActive` 在各任务间一致；`TH` 在 Task 3 Step 1 定义、Task 1 Step 4 仅做存在性调用（`typeof applyThemeVars==='function'`）安全。无占位符。
